language: php
fprocess: php index.php
welcome_message: |
  You have created a new function which uses PHP 7.2.
  Dependencies and extensions can be added using the composer.yml
  and php-extension.sh files.
